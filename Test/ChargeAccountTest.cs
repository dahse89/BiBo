using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Collections.Generic;
using BiBo.Persons;
using BiBo;
using System;

namespace Test
{
    [TestClass()]
    public class ChargeAccountTest
    {
        [TestMethod()]
        public void AccountTest()
        {
          /*
            DateTime changedAt = DateTime.Now;
            List<decimal> changeValues = new List<decimal>(3) { 25, 20, 5};
            decimal currentValue = 50;

            ulong cusID = 1;
            string firstName = "Vorname";
            string lastName = "Nachname";
            DateTime birthDate = DateTime.Parse("01.04.1999");

            
            Customer cus = new Customer(cusID, firstName, lastName, birthDate);
            ChargeAccount acc = new ChargeAccount(cus, changedAt, changeValues, currentValue);
            cus.ChargeAccount = acc;

            Assert.IsTrue(acc.Customer == cus);
            Assert.IsTrue(acc.ChangeAt == changedAt);
            Assert.IsTrue(acc.ChangeValues == changeValues);
            Assert.IsTrue(acc.CurrentValue == currentValue);

            Assert.AreEqual(cus, acc.Customer);
            Assert.AreEqual(cus.ChargeAccount.ChangeAt, acc.ChangeAt);
            Assert.AreEqual(cus.ChargeAccount.ChangeValues, acc.ChangeValues);
            Assert.AreEqual(cus.ChargeAccount.CurrentValue, acc.CurrentValue);
           */
        }
    }
}
